---
declared-services:
  mongo_shared:
    label: mongodb
    plan: v3.0-dev
applications:
- name: mongo-express-shared
  memory: 128M
  disk_quota: 256M
  buildpack: https://github.com/cloudfoundry/nodejs-buildpack
  services:
    - mongo_shared
  env:
    NODE_ENV: cloud-foundry
